#include <string>
#include <vector>

using namespace std;

bool solution(string s) {
    bool answer = true;
    if(s.size() == 4){}
    else if (s.size() == 6){}
    else {return false;}


    for(char c : s){
        if(!isdigit(c)){return false ;}
            else{}        
    }
    
   
    
    return answer;
}